{
  "name": "pssi",
  "version": "0.1.0",
  "description": "Pleading Sanity System Intelligence – AI optimizer for Windows, Android, and handhelds",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm -C app/server dev\" \"pnpm -C app/renderer dev\" \"pnpm run electron:dev\"",
    "electron:dev": "electron app/electron/main.ts",
    "build": "pnpm -C app/renderer build && pnpm -C app/server build && pnpm run build:electron",
    "build:electron": "tsc -p app/electron/tsconfig.json",
    "start": "electron .",
    "test": "pnpm -C app/server test",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "concurrently": "^8.2.2",
    "electron": "^28.0.0",
    "eslint": "^8.55.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {}
}
